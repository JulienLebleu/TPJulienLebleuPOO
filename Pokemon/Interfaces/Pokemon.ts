export default interface Pokemon{
    /* attaquer(cible: Pokemon): void {
        switch (cible.type) {
        case cible.type = 'Eau': {
            this.calculerDegatsContreEau()
        break;
        }
        case cible.type = 'Feu': {
            this.calculerDegatsContreFeu()
        break;
        }
        case cible.type = 'Plante': {
            this.calculerDegatsContrePlante()
        break;
        }
        }
        } */
}